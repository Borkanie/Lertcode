#ifndef SUM_OF_NUM_H
#define SUM_OF_NUM_H
#include <string>
class Solution
{
    public: 
        /// @brief Given a non-negative integer num, return true if num can be expressed as the sum of any non-negative integer and its reverse, or false otherwise.
        /// @param s 
        /// @return 
        bool sumOfNumberAndReverse(int num);
        int reverse(int num);
};

#endif
